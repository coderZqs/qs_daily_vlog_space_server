export type LoginParams = {
  mobile: string;
  password: string;
};

export type RegisterParams = {
  username: string;
  password: string;
  mobile: string;
};

export type User = {
  mobile: string;
};
