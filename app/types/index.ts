// 邀请枚举
export enum InvitationType {
  agree = 1,
  reject = 2,
}

// 评论类型枚举
export enum CommentType {
  comment = 1,
  reply = 2,
}

// 聊天类型枚举
export enum ChatCateogryType {
  group = 1,
  friend = 2,
}

// 消息类型枚举
export enum MsgType {
  TEXT = 1,
  PICTURE = 2,
  VOICE = 3,
}
